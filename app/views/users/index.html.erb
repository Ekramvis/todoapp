<h1>All Users</h1>

<ul>
	<% @users.each do |user| %>
		<li>
			<%= user.name %>
			<!-- REV: I would suggest link_to user.name, user_url(user.id) -->
		</li>
	<% end %>
</ul>
<!-- Also
	link_to 'Home', root_url
	would be cool, you can put it in application.html.erb so its on every page.
	-->